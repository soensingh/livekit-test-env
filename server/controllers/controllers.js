const roomController = require('./room.controller');
const signalingController = require('./signaling.controller');
const adminController = require('./admin.controller');

module.exports = {
	roomController,
	signalingController,
	adminController,
};
