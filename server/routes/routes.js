const { roomRoutes } = require('./room.route');
const { signalingRoutes } = require('./signaling.route');
const { adminRoutes } = require('./admin.route');

module.exports = {
	roomRoutes,
	signalingRoutes,
	adminRoutes,
};
